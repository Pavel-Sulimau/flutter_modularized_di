name: data
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cross_cutting_concerns:
    path: ../cross_cutting_concerns
  domain:
    path: ../domain
  shared_preferences: ^2.2.2

dev_dependencies:
  lints: ^3.0.0
